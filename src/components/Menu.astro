---
// Componente de menú principal
---

<div id="menu-component" class="hidden">
  <div class="my-4 grid grid-cols-1 gap-4 md:grid-cols-2">
    <div class="menu-item" data-option="1">
      <span class="text-terminal-bright">[1]</span> Noticias Tecnológicas
      <div class="ml-4 text-xs text-terminal-dim">Cosmos e Inteligencia Artificial</div>
    </div>

    <div class="menu-item" data-option="2">
      <span class="text-terminal-bright">[2]</span> Currículum de Gusi
      <div class="ml-4 text-xs text-terminal-dim">Perfil profesional</div>
    </div>

    <div class="menu-item" data-option="3">
      <span class="text-terminal-bright">[3]</span> Proyectos Debussy
      <div class="ml-4 text-xs text-terminal-dim">Portfolio de desarrollo web</div>
    </div>

    <div class="menu-item" data-option="4">
      <span class="text-terminal-bright">[4]</span> Juegos Retro
      <div class="ml-4 text-xs text-terminal-dim">Snake, Ahorcado, Tres en Raya</div>
    </div>

    <div class="menu-item" data-option="5">
      <span class="text-terminal-bright">[5]</span> Calculadora Cósmica
      <div class="ml-4 text-xs text-terminal-dim">Edad planetaria y datos NASA</div>
    </div>

    <div class="menu-item" data-option="6">
      <span class="text-terminal-bright">[6]</span> Salir
      <div class="ml-4 text-xs text-terminal-dim">Cerrar sesión</div>
    </div>
  </div>

  <div class="mt-4 text-sm text-terminal-dim">
    Selecciona una opción escribiendo el número o el nombre del comando.
  </div>
</div>

<script>
  document.addEventListener('loadMenu', () => {
    const menuContainer = document.getElementById('menu-container');
    const menuComponent = document.getElementById('menu-component');

    if (menuContainer && menuComponent) {
      menuContainer.innerHTML = menuComponent.innerHTML;

      // Añadir event listeners a las opciones del menú
      const menuItems = menuContainer.querySelectorAll('.menu-item');
      menuItems.forEach(item => {
        item.addEventListener('click', () => {
          const option = item.getAttribute('data-option');
          const input = document.getElementById('terminal-input') as HTMLInputElement;
          if (input && option) {
            input.value = option;
            input.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter' }));
          }
        });
      });
    }
  });
</script>

<style>
  /* Estilos adicionales para el menú si es necesario */
</style>
